# 契约登记表

> 本页是**服务端接口契约的唯一真理**。实现与它冲突时，以本页为准；本页没写的，
> 就是**还没定**，不要在实现里"先随便定一个"。

## 为什么有这一页

服务端的 CLAUDE.md（铁律四）原本把 `magirecocn-architecture-protocol-document`
指为唯一真理。**那个仓库不存在**——它要么从未建起来，要么在文档统一到本站时被并了
进来而没人改指针。结果是：铁律四指向一个空地址，而 `R2` 这样的待决项编号散落在
代码注释与文档正文里，没有任何一处能查到 R1 是什么、R2 定了没有、谁在等谁。

那种状态比没有规矩更糟：它看起来有规矩。所以把登记表落在这里，与协议正文同仓库、
同一次构建验证，不再有跨仓库指针。

::: tip 这一页只登记「状态」，不重复「内容」
字段与语义写在各自的协议页（[API 服务端](/protocol/api-server)、
[客户端握手](/protocol/client-server)）。本页只回答三个问题:**定了没有、
谁在等它、临时值有没有守卫**。
:::

## 状态标记

| 标记 | 含义 | 实现侧可以做什么 |
|---|---|---|
| ✅ **已定** | 线格式冻结。改动走「先加新字段 → 客户端适配 → 双跑过渡 → 再废旧」 | 照着实现 |
| 📝 **草案** | 形状已写出但未冻结，可能整体推翻 | 可以实现，但**必须受生产守卫管辖** |
| 🚧 **保留** | 明令未定 | **禁止自行发挥**。挡路时来把它定掉，不要在实现里绕过去 |

::: danger 📝 与 🚧 都不得裸奔进生产
草案与保留项的共同危险不是"它们存在"，而是**它们可能不被发现地留在生产里**。
一个只含 `path` 的清单在生产跑得好好的，直到某天需要靠内容哈希做缓存失效，
才发现它从来没有过。

所以凡是 📝 的形状，服务端侧必须挂 [`CNV_DEV_MODE` 生产守卫](/deploy/configuration#cnv-dev-mode-生产守卫-🔒)：
忘了配的后果是**功能不可用（显眼）**，而不是临时值悄悄泄进生产（不显眼）。
:::

## 待决项登记

| 编号 | 题目 | 状态 | 谁在等 |
|---|---|---|---|
| **R1** | 协议版本协商与 `protocol_versions` 集合语义 | ✅ 已定 | — |
| **R2** | `/client/scene-manifest` 的清单形状 | ✅ 已定（2026-08） | — |
| **R3** | `/magica/api/page/*` 的聚合响应契约（93 个端点） | 🚧 保留 | 网页前端 · 服务端 |
| **R4** | 战斗结算的服务端裁定边界（伤害/回合/掉落） | ✅ 已定（2026-08） | — |
| **R5a** | 场景资产清单的来源、格式与版本化 | ✅ 已定（2026-08） | — |
| **R5b** | 战斗数值 master data 的来源与版本化 | ✅ 已定（2026-08） | — |

### R1 · 协议版本协商 ✅

`/client/init` 请求带 `protocol_versions:[int]`，服务端在交集中选一个回
`protocol_version`；无交集则握手失败（`400 protocol_version_unsupported`），
**客户端不得降级尝试**。缺省视为 `[1]`。详见 [API 服务端协议](/protocol/api-server)。

### R2 · 场景清单形状 ✅

2026-08 定稿，形状见 [`/client/scene-manifest`](/protocol/api-server#client-scene-manifest)。
定稿要点与推导过程见下节；生产守卫随定稿一并撤除。

### R3 · `/page/*` 聚合契约 🚧

**不要照抄上游。** 上游那 93 个端点占 96% 流量，且是"存档单向膨胀"的根源——
`quest/native/resume/check` 平均 5.3 MB，每次都把 1730 条关卡记录整包重传，
没有分页也没有增量（见[上游 API 清单](/protocol/upstream-api)）。

前端这次由我们自己写，**可以改成分页/增量契约，不必继承这个包袱**。也正因为前端
还没成型，现在定这批形状等于替一个不存在的消费者做决定。等前端把「一屏需要哪些
状态」列出来再定。

### R4 · 战斗结算裁定边界 ✅

现有 Android 生态里，战斗结算由客户端算完上报，服务端既无随机种子也无逐回合轨迹，
因而无法复现或校验——伤害、通关、掉落均可伪造。**新服务端不得重建这个洞。**

#### 决定：按「能不能凭空造出来」分层，而不是按「战斗算得对不对」

| 层 | 谁裁定 | 为什么 |
|---|---|---|
| **掉落 / 货币 / 经验** | **服务端完全裁定**，一概不采信上报 | 这些能凭空造出来，直接进经济 |
| **通关与否** | 服务端做**上界校验**后采信 | 重放不现实，见下 |
| **战斗过程**（逐回合伤害数字、盘序） | **不裁定，也不存** | 存了没用，而且正是存档单向膨胀的来源 |

#### 为什么不做逐回合轨迹重放

重放要求服务端与客户端两份实现**逐位一致**——同一个种子、同样的结算顺序、同样的
浮点行为。而客户端是**网页端，跑在玩家的浏览器里**：JS 引擎、JIT、浮点细节都不由
我们控制。

::: danger 严格但会误判的反作弊，比宽松但准确的更糟
重放一旦有毫厘偏差，症状是**正常玩家的通关被判为作弊**。那类投诉极难复现——玩家
说不清自己做了什么，而服务端只有一个"对不上"的结论。

误封的代价由无辜的人承担，而作弊者大不了换个号。所以裁定的默认方向是：
**宁可漏判，不可误判。**
:::

还有一层现实判断：这是复刻服，**没有排行榜竞争**。作弊的实际危害不是"别人赢不了
我"，而是经济被凭空造出来的资源污染——那正是上表第一行完全交给服务端的原因。
把工程量压在真正有危害的地方，而不是平均分配。

#### 上界校验查什么

都能从 [R5b](#r5b-战斗数值-master-data-✅) 的数值算出来：

| 检查项 | 挡住什么 |
|---|---|
| 队伍成员**属于该账号** | 借用未拥有的角色/记忆结晶。这条客户端完全无法保证（铁律一） |
| 关卡**已解锁** | 跳关直取高级掉落 |
| 回合数 ≤ 关卡上限 | 无限回合刷 |
| 总伤害 ≤ 队伍理论最大单回合输出 × 回合数 × 宽容系数 | 伤害填 999999999 |
| 耗时 ≥ 下限 | 脚本瞬间通关 |
| 同一关卡的通关频率上限 | 刷请求 |

宽容系数是**有意留的余量**：理论最大输出的计算必然比实际偏保守（buff 叠加、暴击、
连携的组合爆炸算不全），卡太死就会误判。宁可让一个作弊者卡在 1.5 倍上界之内，
也不要让一个欧皇的暴击连携被判作弊。

::: warning 上界依赖的数值必须以抓包为准
裁定上界若建立在一个抄错的 Wiki 数值上，表现是某些正常通关被误判为作弊——
又回到上面那个极难复现的投诉。[R5b](#r5b-战斗数值-master-data-✅) 里
「冲突时以抓包为准」那一条，落地时最要紧的就是这里。
:::

#### 还缺什么

上表里「关卡上限」「耗时下限」需要 **quest master data**（关卡定义、敌人数值），
它不在 R5b 已定的 chara / memoria 两类里。这不影响 R4 定稿——策略已定，
可实现的部分（归属校验、频率上限）不依赖它；依赖它的那几项等 quest 数据接入即可
按同一套策略补上。

### R5 拆成了 R5a 与 R5b

原来的 R5 是「master data 的来源、版本化与下发方式」。清点现有资料时发现，
**这一个编号里塞了两个独立的问题**，而它们的成熟度差了一整个数量级：

| | 需要什么 | 现状 |
|---|---|---|
| **资产清单**（哪个场景要哪些文件） | `path` + 内容哈希 + 大小 | 格式可定，管线待接 |
| **战斗数值**（cards / pieces / enemies） | `verbCode` / `effectCode` / `targetId`、成长曲线 | 两个来源互补，见 R5b |

两者的产出物、更新节奏与校验方式都不一样，绑在同一个编号下只会让先具备条件的那个
陪着另一个一起等。拆开之后各自按自己的节奏定。

### R5a · 场景资产清单 ✅

清单是**构建管线的产物**，随部署挂载或同步，**不入版本库**（铁律三）。服务端侧
只有加载器：`internal/scenemanifest` 的 `LoadFile`，由 `CNV_SCENE_MANIFEST_FILE`
指定路径。

```json
{
  "version": "2026-08-01",
  "scenes": {
    "story/11011": [
      { "path": "resource/chara/1001/base.moc3", "sha256": "…", "size": 412849 }
    ]
  }
}
```

`scenes` 的值就是 [R2 定稿](#r2-定稿-清单形状)的资产三元组——**故意与线格式同构**，
让加载器不必做转换，也让"发出去的"与"文件里写的"在评审时能直接对上。

::: tip 版本化沿用资料库已有的做法
`magireco-wiki-data` 的 `catalog.json` 给每个数据集记 `{bytes, records, sha256}`，
`media_manifest.json` 给每个文件记 `{path, sha256, size}`——**这套三元组在这个项目
里已经是既成事实**，30,286 个已镜像文件都按它记着。场景清单沿用它，而不是另发明
一套。

`version` 字段不做格式约束（建议用构建日期或内容哈希），它只进日志，供对账用。
:::

**全量校验在加载时一次做完，失败拒绝启动。** 校验项：`scene_id` 命名空间格式、
`sha256` 须为 64 位小写十六进制、`size` 非负、同场景内 `path` 不重复、资产列表
非空。

::: danger 为什么加载失败要拒绝启动，而不是降级成"该功能不可用"
配了路径说明运维打算启用它。这时候静默跳过，服务看起来一切正常，而所有场景加载
都 503——那种故障要等玩家报上来才发现。

同理，**空的资产列表一律拒绝**：它的语义是"该场景不需要任何资产"，而这几乎总是
构建管线出了错。放行的话客户端会静默进入一个残缺场景，那是整条链路上最难排查的
一类故障。真的不需要资产，就从清单里删掉那个场景。
:::

### R5b · 战斗数值 master data ✅

**两个来源互补，都已具备。**

| 来源 | 提供什么 | 规模 |
|---|---|---|
| `magireco-wiki-data`（Wiki 标定数值） | 人类校对过的数值表：分稀有度的 HP/ATK/DEF 区间、属性、成长类型、行动盘配置、magia / doppel 的效果值与成长率、发动率、持续回合 | 253 个角色数据表 + 1,404 条记忆结晶 |
| `puella-historia`（历史流量归档） | 上游**实际下发**的形态与有界词汇表：`code`(15) / `target`(7) / `sub`(60)，以及 `quest/native/get` 的完整响应结构 | 24,116 次真实调用 |

::: tip 为什么必须两个都要
单靠 Wiki：数值是对的，但形态是**中文散文**——"（全体）属性强化伤害，效果值：305%，
成长：10%"。服务端要下发的是 `{"code":…,"target":…,"sub":…,"effect":305,"growPoint":10}`。

单靠抓包：形态是对的，但里面是**解析后的结果**（服务端算好推下来的 `artList`），
反推不出成长曲线与未出现过的组合。

合起来才完整：**抓包钉住 schema 与枚举，Wiki 提供数值**。而且抓包把词汇表限成
有界集合（15 × 7 × 60），所以中文描述 → 结构化字段是一次**受限映射**，
不是开放式自然语言解析。
:::

**接合点**：角色用 `charaId`（`characters.json` 已带，如 七海八千代 = 1002），
记忆结晶用 `number` 与 `effect_icon`。两侧都能对上游 ID 对齐。

**提取状态不一致，要留意**：

- 记忆结晶**已经是结构化 JSON**（`memoria.json` 的 `atk_min`/`atk_max`/`hp_*`/`def_*`/
  `cooldown`/`effect_detail` 等字段），可直接消费；
- 角色数值**还在 wikitext 模板里**（`Template:角色数据表/<角色名>`，253 个），
  字段名是中文（`HP最小3`/`ATK最大5`/`magia效果详细5`/`盘`）。提取器已就位：
  api-server 的 `cmd/masterdata-extract`。

#### 效果名映射表：机械部分已自动化，领域判断留给人

提取器把结构拆干净（模板参数、分稀有度档位、中文效果串里的目标/效果值/成长/发动率/
持续回合），但**「属性强化伤害」对应抓包的哪个 `code`/`sub` 不由它决定**——那是领域
判断，不该藏在一个 switch 里由写代码的人独自拍板。

映射表因此是**外部数据文件**（`cmd/masterdata-extract/effects.example.json`），
可评审、可 diff、可逐条讨论。

**当前状态：222 个效果名待映射。** 示例表已种入 65 条命名规则可推导的
（`UP`→`BUFF`、`DOWN`→`DEBUFF`、`回复`→`HEAL`、`伤害`→`ATTACK`），
`sub` **一律留空**——那 60 值词汇表尚不完整，编不得。`Art.Sub` 是可选且不做封闭
校验的，将来补上是纯增量，不会破坏已产出的数据。

::: danger 遇到没映射的效果名一律失败，不静默跳过
跳过的后果是**一份看起来正常、实则缺了效果的 master data**：服务端照常启动、战斗
照常打，只是某些技能悄悄没有了——要靠玩家察觉"这技能好像没生效"才会暴露。

提取器失败时按出现频次降序打出缺口清单，便于优先补高频的那些
（`-report-gaps` 只看清单不产出数据）。
:::

`target` 的映射相反，**写死在代码里**：14 种 Wiki 写法 → 7 值封闭集合的对应是可
判定的，而且目标集合已被抓包钉死。测试钉住"映射表的值必须都落在那 7 个里面"。

**版本化与下发**沿用 R5a 同一套做法：提取产物是**构建管线的产物**，随部署挂载或
同步，**不入版本库**（铁律三）；仓库里只有提取脚本与加载器。数据集级用
`catalog.json` 那套 `{bytes, records, sha256}` 记账。

::: warning 数值是"标定值"，不是权威源
Wiki 的数值由人工校对，可能有个别错漏；抓包只覆盖 2024-06 至 2024-07 的实际下发。
两者对不上时，**以抓包为准**——它是上游真实行为的证据，而 Wiki 是对它的转录。

这一条在 R4（战斗裁定边界）落地时尤其要紧：裁定上界若建立在一个抄错的数值上，
表现是某些正常通关被误判为作弊，而那种投诉极难复现。
:::

## R2 定稿：清单形状

### 字段

```json
{
  "success": true,
  "scene_id": "story/11011",
  "manifest_hash": "9f8e7d6c…",
  "assets": [
    { "path": "resource/chara/1001/base.moc3", "sha256": "3c0a9b8d…", "size": 412849 }
  ]
}
```

| 字段 | 类型 | 为什么需要它 |
|---|---|---|
| `path` | string | 边缘节点上的 key。客户端把它拼到按 `caps:["resource"]` 选出的节点 base 上 |
| `sha256` | string | **缓存失效判据 + 完整性校验**，见下 |
| `size` | int64 | 调度用（并发与进度），也是下载前一道廉价 sanity check：长度对不上就不必算哈希了 |
| `manifest_hash` | string | 整张清单的哈希，用于跳过重复传输，见[增量](#增量-清单自身也可以不传) |

### 为什么是 `sha256` 而不是 ETag

边缘节点**刻意不发 `ETag`**——S3 的 ETag 是文件 MD5，为了一次清单去读完整棵资产树
算摘要，代价与收益完全不成比例（见[资产分发面](/contributing/server/resource-plane)）。
所以缓存失效的判据必须由清单这一侧给出。

而既然要给，就给 sha256 而不是更便宜的 CRC32/MD5——因为它同时承担**完整性校验**：

::: danger 边缘节点是信任树的最外层
边缘节点持小时级证书、可能是第三方镜像。客户端从它那里取来的字节，必须能对着一个
**由业务节点签发路径下发的**哈希核对。这里抗碰撞有真实的安全意义，不只是防传输错误
——所以是 sha256，不是 CRC32。
:::

### 清单里**不放 URL**

只给 `path`，不给完整 URL。客户端从[签名节点目录](/protocol/client-server#签名节点目录)
里按 `caps` 含 `resource` 选节点。把 URL 钉进清单等于把线路选择固化在一份可能被缓存
很久的数据里，多节点、故障转移、就近接入全都作废。

### `scene_id` 命名空间

```
<namespace>/<id>
```

- `namespace`：`^[a-z][a-z0-9_]*$`，保留取值 `story` `battle` `chara` `ui` `movie` `audio`；
- `id`：`^[A-Za-z0-9_.-]{1,64}$`。

::: warning 服务端必须校验格式，即便当前实现是查表
现在 `scene_id` 只用于查表，不拼进文件路径，所以畸形值不危险。**但契约要钉住格式**
——一旦将来有人改成按约定拼路径（很自然的优化），没有格式校验的 `scene_id` 就是一个
现成的路径穿越入口。在它还不危险的时候把闸设上，比等它危险了再补要便宜得多。
:::

### 增量：清单自身也可以不传

响应里的 `manifest_hash` 让客户端能缓存整张清单：

```json
// 请求带上手里那份的哈希
{ "scene_id": "story/11011", "known_manifest_hash": "9f8e7d6c…" }

// 一致时服务端省掉整张清单
{ "success": true, "scene_id": "story/11011", "manifest_hash": "9f8e7d6c…", "unchanged": true }
```

**服务端不跟踪客户端有什么。** 差集完全在客户端做（它本来就有本地缓存索引），
服务端只回答"这张清单还是不是你手里那张"。这与"边缘节点不理解游戏结构"是同一条
原则：状态留在最靠近它的一侧。

`unchanged:true` 时 `assets` **省略**（不是空数组）——空数组的语义是"该场景不需要
任何资产"，与"你手里那份还有效"完全不同，不能共用一种表示。

### 失败分支

| 状态码 | 错误码 | 含义 |
|---|---|---|
| `400` | `missing_scene_id` | 请求没带 `scene_id` |
| `400` | `malformed_scene_id` | `scene_id` 不符合命名空间格式 |
| `404` | `scene_not_found` | 未知的 `scene_id` |
| `503` | `manifest_unavailable` | 场景清单尚未接入构建管线 |

::: tip 503 而不是空清单
未接入时**明确报错**，不返回 `assets: []`。空清单会被客户端理解为"该场景无需任何
资产"，于是静默进入一个残缺的场景——错误被推迟到最难排查的地方才暴露。
:::

## 改这一页的规矩

- 把某项从 🚧 改成 ✅ 之前，**形状必须已经写进对应协议页**，本页只留状态与索引；
- ✅ 之后再改线格式，走「先加新字段 → 客户端适配 → 双跑过渡 → 再废旧」，
  且要在[契约纪律](/contributing/server/discipline)里登记影响面；
- 📝 转 ✅ 时，记得**撤掉对应的生产守卫**——守卫留着不撤，等于永久禁用一个已经定稿
  的功能，而且没人知道为什么。
